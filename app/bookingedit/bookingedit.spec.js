'use strict';

describe('myApp.bookingedit module', function() {

    beforeEach(module('myApp.bookingedit'));

    describe('bookingedit controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var bookingedit = $controller('BookingeditCtrl');
            expect(bookingeditCtrl).toBeDefined();
        }));

    });
});
