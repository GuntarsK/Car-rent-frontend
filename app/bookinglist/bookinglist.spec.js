'use strict';

describe('myApp.bookinglist module', function() {

    beforeEach(module('myApp.bookinglist'));

    describe('bookinglist controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var bookinglistCtrl = $controller('BookinglistCtrl');
            expect(bookinglistCtrl).toBeDefined();
        }));

    });
});
