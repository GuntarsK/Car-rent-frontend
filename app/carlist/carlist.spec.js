'use strict';

describe('myApp.carlist module', function() {

    beforeEach(module('myApp.carlist'));

    describe('carlist controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var carlistCtrl = $controller('CarlistCtrl');
            expect(carlistCtrl).toBeDefined();
        }));

    });
});
